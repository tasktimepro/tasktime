services:
  app:
    build: .
    container_name: tasktime
    command: ["sh", "-lc", "sh ./scripts/start-dev-servers.sh"]
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "3101:3101"

volumes:
  node_modules:

